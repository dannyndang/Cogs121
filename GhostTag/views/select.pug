extends layout

block content
    h1(class='testHome text-center') #[strong Welcome to Ghost Tag!]
    h3(class='text-center') Let's Get Started!   

    p(class='text-center') Click to record a route, and have a seeker try to find you

    button(type="button" class="btn btn-success center-block" id = 'RecordId') Record

    p(class='text-center') Click to find a route that a seeker is currenty on and try to find him

    button(type="button" class="btn btn-success center-block" id = 'FindId') Find


    script(type='text/javascript').
        window.onload = function () {
            document.getElementById("RecordId").onclick = function(){
                location.href = "/record" 
            };
            document.getElementById("FindId").onclick = function()
            {
                location.href = "/find"
                }
        };
